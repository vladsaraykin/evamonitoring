CREATE TABLE IF NOT EXISTS server (id SERIAL PRIMARY KEY, url VARCHAR(255));
CREATE TABLE IF NOT EXISTS telegram_user (id SERIAL PRIMARY KEY, user_id SERIAL UNIQUE, username VARCHAR(255), status VARCHAR (30));